"""Data exfiltration evals (EVALS.md section 3, threat T3).

Measures: leak rate via corpus dump, tool argument smuggling, URL encoding.
Stub: returns no results until guardrails are implemented.
"""

from __future__ import annotations

from evals.harness import EvalResult


def run() -> list[EvalResult]:
    """Stub. Returns empty until guardrails + tools are wired."""
    return []
